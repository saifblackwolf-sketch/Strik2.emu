workflows:
  strik2-m4-build:
    name: Strik2.emu M4 Official Flow
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      xcode: latest
    scripts:
      - name: Fetch Submodules
        script: |
          # هذا الأمر يحل مشكلة ملف Pcsx2Utils المفقود في السجل
          git submodule update --init --recursive

      - name: M4 Display Patch
        script: |
          # تطبيق نسبة 1.432f لتناسب شاشة iPad Pro M4
          FILE=$(find . -name "GSRenderer.cpp" | head -n 1)
          if [ -f "$FILE" ]; then
            sed -i '' 's/1.333f/1.432f/g' "$FILE"
            echo "M4 Patch applied"
          fi

      - name: CMake Configure
        script: |
          # استخدام الإعدادات الرسمية من ملف BUILD_IOS.md
          # وتوجيه البحث لمجلد src/cpp حيث يوجد ملف CMakeLists.txt
          cmake -S src/cpp -B build/ios_device -G Xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=16.0 \
            -DiPSX2_REAL_DEVICE=ON

      - name: Build and Package
        script: |
          # تنفيذ أمر البناء الرسمي وتجميع الـ IPA
          cmake --build build/ios_device --target iPSX2 --config Release
          cd build/ios_device
          mkdir -p Payload
          cp -R Release-iphoneos/iPSX2.app Payload/
          zip -qr iPSX2_M4_Strik2.ipa Payload/

    artifacts:
      - build/ios_device/*.ipa
